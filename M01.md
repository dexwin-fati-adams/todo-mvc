Milestone Plan — M01
Milestone details
Item
Value
Milestone
M01 — Repository Setup
Capability
Todo MVC repository and pnpm workspace setup
Branch name
chore/m02-repository-setup
Planned submission date
2026-08-12

Requirements
Link to the exact requirement section
Repository Setup requirement section
Objective
What will this milestone achieve?
•    Set up the Todo MVC GitHub repository.
•    Set up the repository as a pnpm workspace.
•    Create the backend, frontend, and shared contracts folders.
•    Create the required root configuration files.
Included
•    Create the todo-mvc GitHub repository.
•    Connect to the Dexwin environment using SSH.
•    Clone the GitHub repository.
•    Enter the repository directory.
•    Create the M02 branch.
•    Check the Git branch.
•    Check Node.js.
•    Check pnpm.
•    Initialize the root package.
•    Create pnpm-workspace.yaml.
•    Configure apps/* as workspace packages.
•    Configure packages/* as workspace packages.
•    Configure allowBuilds for esbuild.
•    Create apps/api.
•    Create apps/web.
•    Create packages/contracts.
•    Set up the contracts package.
•    Add shared types and schemas to the contracts package.
•    Create the required root configuration files.
•    Verify the workspace setup.
•    Commit and push the changes.
Not included
•    Todo CRUD functionality.
•    Backend API implementation.
•    Database implementation.
•    Authentication.
•    Frontend pages.
•    Frontend UI.
•    Business logic.
•    Deployment.
Current repository evidence
What files, commands, or existing patterns did you inspect before making this plan?
I created a new GitHub repository named todo-mvc.
The repository is: https://github.com/dexwin-tech-ltd/todo-mvc.git
I connected to the Dexwin environment using SSH: ssh fati.adams@eng.dexwin.net
I cloned the repository: git clone https://github.com/dexwin-tech-ltd/todo-mvc.git
I entered the repository: cd todo-mvc
I created the M02 branch: git checkout -b chore/m02-repository-setup
I checked the branch: git branch
I checked Node.js: node --version
I checked pnpm: pnpm --version
I entered the local repository: cd ~/Desktop/todo-mvc
I initialized the root package: pnpm init
This created: package.json
 
Acceptance criteria
☐ The todo-mvc repository exists.
☐ The repository is cloned locally.
☐ The chore/m01-repository-setup branch exists.
☐ The root package.json exists.
☐ pnpm-workspace.yaml exists.
☐ apps/api exists.
☐ apps/web exists.
☐ packages/contracts exists.
☐ The contracts package contains the shared data definitions.
☐ apps/* and packages/* are configured as workspace packages.
☐ The required root configuration files exist.
☐ pnpm install completes successfully.
Contracts
Input
What information enters the operation?
The workspace receives the repository structure and pnpm workspace configuration.
The workspace configuration is:
packages:
   - 'apps/*'
   - 'packages/*'
 allowBuilds:
   esbuild: true
esbuild is a tool that helps JavaScript and TypeScript code get built into code that can run.
The packages section tells pnpm where the workspace packages are located.
apps/* contains: apps/api, apps/web
packages/* contains: packages/contracts
The contracts package contains shared data definitions. These can include types, schemas, shared request definitions, and shared response definitions. The API and web applications can use the same definitions.
Success
What exact value or visible result means success?
•    The repository has the required structure.
•    The workspace is recognized by pnpm.
•    The contracts package exists.
•    The dependencies install successfully.
The final structure is:
todo-mvc/
 ├── apps/
 │   ├── api/
 │   └── web/
 ├── packages/
 │   └── contracts/
 ├── package.json
 ├── pnpm-workspace.yaml
 ├── tsconfig.json
 ├── eslint.config.js
 ├── .prettierrc
 ├── .gitignore
 └── README.md
Expected failures
What expected failures can happen at this boundary?
Failure
When it happens
How it is represented
Invalid workspace configuration
pnpm-workspace.yaml has incorrect syntax
pnpm shows an error
Missing workspace package
A required folder is missing
pnpm does not detect the package
Dependency installation failure
pnpm install cannot install a dependency
pnpm shows an error
esbuild build blocked
Its build script is not allowed
pnpm shows a warning or error
Wrong Git branch
Work is done on the wrong branch
git branch shows the wrong branch

Unexpected failures
What unexpected failures can reach this boundary? How will they be made safe for the caller or user?
Unexpected problems may come from the computer, network, GitHub, or package registry. If a command fails, I will read the terminal error, fix the problem, and run the command again before continuing.
Responsibilities
State what each relevant layer owns. Write Not applicable with a reason for unused layers.
Layer
Responsibility
Contract
Create packages/contracts for shared types and schemas.
Route or page
Not applicable. No routes or pages are implemented.
Service or flow
Not applicable. No business logic is implemented.
Repository or API adapter
Not applicable. No database or API adapter is implemented.
Hook
Not applicable. No frontend hooks are implemented.
View
Not applicable. No frontend UI is implemented.

Certainty checks
1. What information enters the system?
The repository structure and pnpm workspace configuration.
2. How will you check that information?
I will check the files and directories. I will also run pnpm commands from the repository root.
3. What will the operation return when it succeeds?
pnpm will recognize the workspace packages. The dependencies will install successfully.
4. What expected failures can happen?
Invalid workspace configuration. Missing workspace package. Dependency installation failure. esbuild build failure. Wrong Git branch.
5. How will each expected failure be represented?
Git and pnpm will show errors or warnings in the terminal.
6. Where will the route, business rules, and database or UI work live?
Not applicable. This milestone only creates the repository foundation.
7. What tests will you write before implementation?
I will check that the required folders and configuration files exist. I will verify that pnpm recognizes the workspace.
8. How will you prove the whole operation works through its real boundaries?
I will run: pnpm install, from the repository root. I will confirm that pnpm reads the workspace configuration and completes successfully.
9. Does this change multiple records or call another system?
No. This milestone creates repository files and directories.
10. If this is frontend work, what loading, empty, ready, error, pending, and accessibility states are needed?
Not applicable. No frontend feature is implemented.
Tests to write first
List tests in the order you will write them. Include success and expected failure cases.
•    Check that the current branch is chore/m02-repository-setup.
•    Check that apps/api exists.
•    Check that apps/web exists.
•    Check that packages/contracts exists.
•    Check that pnpm-workspace.yaml exists.
•    Check that apps/* is configured as a workspace.
•    Check that packages/* is configured as a workspace.
•    Check that esbuild is allowed to run its build script.
•    Run pnpm install.
•    Check that the workspace packages are detected.
Expected first failure
What exact behavioural failure should the first test show before implementation exists?
The first workspace check should fail because the workspace has not been set up yet. After I create the workspace structure and configuration, I will check again and it should work.
Files expected to change
File
Reason
package.json
Created by pnpm init for the root package configuration.
pnpm-workspace.yaml
Defines the workspace packages and esbuild permission.
apps/api/
Holds the backend application.
apps/web/
Holds the frontend application.
packages/contracts/
Holds shared types and schemas.
packages/contracts/package.json
Defines the contracts workspace package.
packages/contracts/src/
Holds the shared contracts.
tsconfig.json
Defines the TypeScript configuration.
eslint.config.js
Defines the ESLint configuration.
.prettierrc
Defines the Prettier configuration.
.gitignore
Defines files Git should ignore.
README.md
Documents the repository.

Implementation steps
Keep each step small and ordered.
1. Create the GitHub repository — I create a new repository named todo-mvc under the Dexwin organization: https://github.com/dexwin-tech-ltd/todo-mvc.git
2. Connect to the Dexwin environment — I connect using SSH: ssh fati.adams@eng.dexwin.net. SSH provides an encrypted connection to the remote environment.
3. Clone the repository — git clone https://github.com/dexwin-tech-ltd/todo-mvc.git
4. Enter the repository — cd todo-mvc
5. Create the M02 branch — git checkout -b chore/m02-repository-setup
6. Check the branch — git branch. I confirm that the active branch is * chore/m02-repository-setup
7. Check Node.js — node --version. This confirms that Node.js is installed.
8. Check pnpm — pnpm --version. This confirms that pnpm is installed.
9. Enter the local repository — cd ~/Desktop/todo-mvc
10. Initialize the root package — pnpm init. This creates package.json, which stores the root package information (name, version, scripts, dependencies, and development dependencies).
11. Create the workspace directories — apps/api (backend), apps/web (frontend), packages/contracts (shared types and schemas).
12. Create pnpm-workspace.yaml — I create it in the root directory and add the packages and allowBuilds configuration.
13. Configure the workspace packages — the packages section tells pnpm where apps/api, apps/web, and packages/contracts are located.
14. Configure allowBuilds — I add allowBuilds: esbuild: true so the esbuild dependency can run its build script when required.
15. Create the contracts package — cd packages/contracts, then pnpm init, which creates packages/contracts/package.json.
16. Create the contracts source directory — packages/contracts/src/, which will hold the shared types and schemas.
17. Add the shared contracts — I add the shared data definitions used by the API and web applications (types/, schemas/, index.ts). The exact contract files will follow the project requirements.
18. Create the root configuration files — tsconfig.json, eslint.config.js, .prettierrc, .gitignore, README.md
19. Install dependencies — from the repository root, I run pnpm install and check the terminal output for errors.
20. Verify the workspace — I verify that pnpm recognizes apps/api, apps/web, and packages/contracts, and that the required files and folders exist.
21. Check Git status — git status. I review the files that were created or changed.
22. Commit the setup — I commit the repository setup changes to chore/m02-repository-setup.
23. Push the branch — I push the branch to the Dexwin GitHub repository.
24. Create the Pull Request — I create the Pull Request for M02 repository setup.
Verification commands
•    node --version
•    pnpm --version
•    pnpm install
•    pnpm list --depth -1
•    git branch
•    git status
Risks and assumptions
What are the risks and assumptions?
•    Node.js is installed.
•    pnpm is installed.
•    I have access to the Dexwin environment.
•    I have access to the GitHub repository.
•    The project uses pnpm workspaces.
•    esbuild may be installed by a dependency.
•    The root package scripts will follow the team's agreed convention.
•    No Todo MVC functionality is implemented in this milestone.
Help already received
Record material help used while writing this plan.
•    Research on pnpm workspaces.
•    Research on package.json.
•    Research on pnpm-workspace.yaml.
•    Research on allowBuilds and esbuild.
•    Research on shared contracts packages.
Plan checklist
☐ The objective is small and clear.
☐ The branch name is included.
☐ The plan matches the requirements.
☐ The contracts are exact.
☐ Success and failure paths are covered.
☐ The tests come before implementation.
☐ The files and steps are specific.
☐ No production implementation has started.
☐ Material help and AI use are disclosed.


