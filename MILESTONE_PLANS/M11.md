Milestone Details
Milestone
M11 — Plan todo search
Capability
Search todos by title text
Branch name
feat/m12-search-todos
Planned submission date
27th August 2026
Requirements
https://docs.google.com/document/d/1wutHDaxzwjcLpVUNr1LSWHB2yIQC5biS-5JlCA2TaeQ/edit?tab=t.701x9oh39h6p
Objective
Add title search without adding pagination.
Included
• How the search text is cleaned up before use (trimming).
• The longest search text that is allowed.
• What happens if the search text is blank.
• Matching text inside the title, with no care for upper or lower case.
• How search and status work together at the same time.
Not Included
• Paging, for example page 1, page 2.
• Searching any field other than the title.
• Any frontend or UI work. This plan is for the backend only.
Current Repository Evidence
• The GET /todos route from M08. It reads all todos, in one fixed order, oldest first.
• The status check from M09/M10. It adds a status query field, checked with Zod, and changes the WHERE clause in the repository.
• The todos table. It has a title field, which is what the search will look at.
• db.ts and Drizzle. These build the database query in code, not by joining raw text. This keeps the query safe from bad input.
• The style used so far: route, service, repository, with Zod for checks and Result types for errors. M12 will use the same style.
Acceptance Criteria
• Search matches a case-insensitive title substring. Done.
• Blank search and search text over 100 characters are rejected. Done.
• Status and search combine with AND. Done.
• Query values are handled safely by Drizzle. Done.
• Status matching, ordering, and empty results remain correct. Done.
Contracts
Input
GET /todos
GET /todos?search=milk
GET /todos?search=Milk
GET /todos?status=active&search=milk
Success
200 OK
[ { "id": "uuid", "title": "string", "completed": false, "createdAt": "ISO-8601 timestamp", "updatedAt": "ISO-8601 timestamp" } ]
Expected Failures
Failure
When it happens
How it is shown
BlankSearchValue
search is sent, but it is empty or only spaces after trimming.
Result.err({ type: "BlankSearchValue" }). Returns 400.
SearchValueTooLong
The trimmed search text is longer than the allowed maximum.
Result.err({ type: "SearchValueTooLong", max: 100 }). Returns 400.

Unexpected Failures
• Example: the database is down, or the status-and-search read query fails.
• These are caught at the repository. They become Result.err({ type: "UnexpectedError", cause }). The error is logged. The client only sees a plain 500 message, with no internal details.
• No error should ever escape without being caught. The route uses ts-pattern to handle every possible Result, for the query checks and the database read.
Responsibilities
Layer
What it does
Route
Reads the query string. Checks it with Zod: search must be trimmed, not blank if present, and not too long. Works together with the existing status check. Calls the service with the checked values. Turns the Result into an HTTP response.
Service
Takes the checked search text and status. Asks the repository for the matching todos. Passes the Result through.
Repository
Runs one SELECT on the todos table, using Drizzle to safely add a case-insensitive title match and the existing status check to the WHERE clause. Keeps the same ordering as M08. Catches any database error and turns it into UnexpectedError.
Hook
Not applicable
View
Not applicable

Certainty Checks
What comes into the system?
An optional search query field, and the existing optional status query field, both on GET /todos.
How will it be checked?
A Zod schema checks that search, is present, is trimmed, is not blank, and is not too long.
What does the operation return when it succeeds?
200, with a list of todos that match the search text and the status, if given. Same shape as M08.
What expected failures can happen?
BlankSearchValue, SearchValueTooLong, plus the existing InvalidStatusValue, DuplicateStatusParam, and UnknownQueryParam from M09/M10. All return 400.
How is each expected failure represented?
As a Result.err with a type field, matched exhaustively with ts-pattern at the route, and turned into the response.
Where does the route, business rules, and database work live?
Route: reads and checks the query string, for both search and status. Service: passes the checked values along. Repository: builds the safe database query for both.
What tests were written before implementation?
They cover matching, trimming, blank input, length limits, combining search with status, safe querying, and regression checks.
How is the whole operation proven through its real boundaries?
A real end-to-end test file (todo-status.e2e.test.ts) runs the real app against a real test database, covering both status and search together.
Does this change multiple records or call another system?
Not applicable
If this is frontend work, what states are needed?
Not applicable. This milestone is backend only.
Tests Written
• Repository test
• Service test
• Route test
• End-to-end test (real app, real database)
Expected First Failure
The search query field does not exist yet. Before this milestone, StatusQuerySchema only checks status. So any test sending ?search=... should fail, because the schema does not know about search — either the search text is silently ignored, or the request is wrongly rejected as an unknown query key if the schema is strict. Either way, the test proves search does not work yet, before the search field and the repository logic are added.
Files Changed
File
Implementation
packages/contracts/src/todos/todo.contracts.ts
Added a search field to StatusQuerySchema, so it now checks both status and search on GET /todos.
apps/api/src/modules/todos/todo.repository.ts
Used status and search together in the repository, combined with AND, to find matching todos.
apps/api/src/modules/todos/todo.repository.test.ts
Wrote repository tests for status alone, search alone, and status and search combined.
apps/api/src/modules/todos/todo.service.ts
Connected the repository to the service and passed status and search through.
apps/api/src/modules/todos/todo.service.test.ts
Updated the service test to check that status and search are both passed to the repository.
apps/api/src/modules/todos/todo.route.ts
Connected the service to the route and passed the checked status and search values.
apps/api/src/modules/todos/todo.route.test.ts
Updated the route test to check that status and search are both passed to the service.
apps/api/src/modules/todos/todo-status.e2e.test.ts
Added end-to-end tests for search: matching, case-insensitivity, no matches, blank search, overlong search, and status and search combined.

Implementation Steps
• Added a search field to the existing StatusQuerySchema in the contracts.
• Used status and search together in the repository, combined with AND.
• Wrote repository tests for status, search, and both combined.
• Connected the repository to the service.
• Wrote a service test.
• Connected the service to the route.
• Wrote a route test.
• Wrote end-to-end tests for search using the real app and a real test database.
• Ran the server and tested the API in Postman to make sure everything works well.
Verification Commands
pnpm run typecheck pnpm run lint pnpm run format:check pnpm test
Risks and Assumptions
• Assumes a maximum search length of 100 characters.
• Assumes search and status always combine with AND, never OR, since the requirement only asks for AND.
• Assumes no login is needed for this endpoint, since this whole project has no auth yet.
Help Already Received
None
Plan Checklist
☑ The goal is small and clear.
☑ The branch name is included.
☑ The plan matches the requirements.
☑ The contracts are exact.
☑ Success and failure paths are covered.
☑ The tests are written before the code.
☑ The files and steps are specific.
☑ No real code has been written yet.
☑ Any help used is written down.
